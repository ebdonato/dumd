{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "postinstall": "node ../scripts/install-git-hooks.js",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^3.0.7"
  }
}
